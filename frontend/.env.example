# Desarrollo local: Vite proxifica /api al backend local.
VITE_API_BASE_URL=/api

# Producción: no usar este fallback. El workflow Deploy Azure inyecta
# VITE_API_BASE_URL=https://<container-app-fqdn>/api y el build productivo
# falla si la variable falta, no es absoluta o no usa HTTPS.
